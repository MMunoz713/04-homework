//variables within the quiz
var currentQuestion = 0;
var time = 15 * questions.length;
var answer = document

function startQuiz() {
    var question1
}
function storeScores() {
    localStorage.setItem("pointsArr", JSON.stringify(pointsArr));
}